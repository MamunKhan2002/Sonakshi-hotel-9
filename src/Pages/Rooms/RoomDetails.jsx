
import PropTypes from 'prop-types';

const RoomDetails = () => {

    return (
        <div>
            <h1>Room ID : </h1>
        </div>
    );
};

RoomDetails.propTypes = {
    
};

export default RoomDetails;